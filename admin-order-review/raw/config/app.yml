---
title: ${ADMIN_APP_TITLE}

node: ${MALL_NODE}
debade_secret: ${DEBADE_SECRET}
rpc:
  gateway:
    url: ${GATEWAY_API}
    client_id: ${GATEWAY_CLIENT_ID}
    client_secret: ${GATEWAY_CLIENT_SECRET}
  unitconv:
    url: ${UNITCONV_API}
  chemdb:
    url: ${CHEMDB_API}
  order:
    url: ${LAB_ORDERS_API}
    client_id: ${LAB_ORDERS_GAPPER_ID}
    client_secret: ${LAB_ORDERS_GAPPER_SECRET}

order_review_process: ${ORDER_REVIEW_PROCESS_NAME}
...
